# Future Retro Mode

Reserved for the next phase.

Planned components:

- EmulationStation frontend
- RetroArch
- selected libretro cores and standalone emulators
- controller auto-configuration
- curated per-console game library
- Retro-specific Plymouth splash
- `rental-mode karaoke|retro`
- systemd service isolation so only the purchased package is exposed

Batocera can be used as a reference for emulator configuration and hardware/controller behavior, but Debian remains the base OS.
