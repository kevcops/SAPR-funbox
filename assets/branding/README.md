# Branding

Place the approved SA Party Rental logo here as `logo.png` when available.

The v1 Plymouth theme currently uses a text fallback so the installer can be tested before final logo artwork is added.
